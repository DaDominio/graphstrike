# OpenEnv environment server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# LLM agent (Qwen3 via AWS Bedrock)
boto3>=1.34.0
botocore>=1.34.0

# OpenEnv HTTP client
requests>=2.31.0

# OpenEnv SDK (installed separately — see Dockerfile; falls back to stubs if missing)
# pip install openenv
